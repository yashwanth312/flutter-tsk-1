import 'package:flutter/widgets.dart';

import 'code/ui.dart';

main() {
  runApp(x());
}

// ignore: camel_case_types
class x extends StatelessWidget {
  build(BuildContext c_test) {
    return MyApp();
  }
}
